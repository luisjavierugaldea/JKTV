{
  "name": "stremio-clone",
  "version": "1.0.0",
  "description": "Stremio Clone - Backend + Frontend",
  "private": true,
  "scripts": {
    "dev": "concurrently --kill-others \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "install:all": "npm --prefix backend install && npm --prefix frontend install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
